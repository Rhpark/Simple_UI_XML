<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        Simple_UI_Core 라이브러리 Lint 설정

        이 파일은 라이브러리를 사용하는 앱의 빌드 시 자동으로 적용됩니다.
        라이브러리 내부에서는 BaseSystemService의 tryCatchSystemManager()로
        권한이 없을 때 안전하게 처리하므로, 사용자 앱에서 Lint 경고를 무시합니다.
    -->

    <!--
        권한 관련 Lint 경고 무시 설정
        system_manager 패키지의 모든 Controller와 Info 클래스는
        권한이 없어도 안전하게 동작하도록 설계되었습니다.
    -->

    <!-- MissingPermission: 권한 누락 경고 무시 -->
    <issue id="MissingPermission" severity="ignore">
        <!-- 모든 system_manager 하위 클래스 -->
        <ignore path="**/system_manager/**"/>
    </issue>

    <!-- NotificationPermission: Android 13+ 알림 권한 경고 무시 -->
    <issue id="NotificationPermission" severity="ignore">
        <ignore path="**/controller/notification/**"/>
    </issue>

    <!--
        특수 권한 관련 경고
        BATTERY_STATS는 시스템 앱만 사용 가능하지만,
        라이브러리에서는 권한 없을 때 기본값 반환하므로 안전합니다.
    -->
    <issue id="ProtectedPermissions" severity="ignore">
        <ignore path="**/info/battery/**"/>
    </issue>
</lint>
